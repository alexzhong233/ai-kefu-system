<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aikefu.mapper.ConversationMessageMapper">

    <select id="getMessagesByConversationId" resultType="com.aikefu.entity.ConversationMessage">
        SELECT * FROM t_conversation_message
        WHERE conversation_id = #{conversationId} AND deleted = 0
        ORDER BY created_at ASC
    </select>

    <select id="getRecentMessages" resultType="com.aikefu.entity.ConversationMessage">
        SELECT * FROM t_conversation_message
        WHERE conversation_id = #{conversationId} AND deleted = 0
        ORDER BY created_at DESC LIMIT #{limit}
    </select>

</mapper>
